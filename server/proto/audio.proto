syntax = "proto3";

package audio;

service AudioService {
  rpc UploadAudio (AudioRequest) returns (AudioResponse);
}

message AudioRequest {
  bytes  pcm_data    = 1;
  int32  sample_rate = 2;
  int32  channels    = 3;
  int32  bit_depth   = 4;
}

message AudioResponse {
  bool   success = 1;
  string message = 2;
}
